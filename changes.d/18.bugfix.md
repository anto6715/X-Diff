Comparing netCDF variables no longer fails when dimensions share the same shape but have different names; the name difference is logged at debug instead. Only a true shape (size) mismatch now raises.
