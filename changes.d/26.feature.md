Add `xdiff plot REF CMP -v VAR -o out.png` to render the difference between two netCDF fields to a static PNG/PDF/SVG — one full-size, smoothly shaded map per variable on plain lon/lat axes (latitude-corrected aspect, cropped to the data domain, land drawn from the data's NaN mask), diverging colormap centered at 0 with a robust color limit.
