Add `xdiff plot REF CMP -v VAR -o out.png` to render the difference between two netCDF fields (one full-size map per variable, diverging colormap centered at 0 with a robust color limit; land/masked cells shown grey) to a static PNG/PDF/SVG.
