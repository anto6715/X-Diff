Hardened netCDF comparisons so mismatched dimensions, coordinates, and empty comparison results fail explicitly, fixed `--last-time-step` handling for non-leading time axes, and returned a non-zero CLI status when comparisons fail.
