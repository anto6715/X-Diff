When `xdiff` creates a local Dask cluster via `--dask-workers`, it now prints both the scheduler URL and the browser dashboard link automatically so users can inspect the local cluster without passing an external scheduler.
