Add the live interactive mode to `xdiff plot`: run it without `-o` to serve the plots on a local Panel/Bokeh server (`--port`, `--no-open`). Each variable's difference is shown large and datashaded, so it re-aggregates server-side as you zoom (scroll to zoom) and scales to large grids; a slider adjusts the colour limit live (zoom preserved), land shows grey, and hover reads off values. The reference and comparison maps sit in a collapsed card at the bottom, expanded on demand.
