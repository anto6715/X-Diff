Add the live interactive mode to `xdiff plot`: run it without `-o` to serve the plots on a local Panel/Bokeh server (`--port`, `--no-open`). Each variable's difference is shown large, with scroll-wheel zoom, hover read-out, grey land, and a slider that adjusts the colour limit live (zoom preserved); the reference and comparison maps sit in a collapsed card at the bottom, expanded on demand.
