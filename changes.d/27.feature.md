Add the live interactive mode to `xdiff plot`: run it without `-o` to serve the reference/comparison/difference plots on a local Panel/Bokeh server (`--port`, `--no-open`), with per-variable pan/zoom/hover and a slider that adjusts the difference colour limit live.
